// Exemplo prático de if ternário

//Define a variavél com o dia da semana (0 = Domingo, 1 = Segunda...)

var dia_semana = 5;

//Verificar a condiçãoe define o resultado na variavél 'status_loja'
var status_loja = (dia_semana == 0 || dia_semana == 6) ? "Funcionamos apenas de Segunda à Sexta":"Loja aberta";

//Imprime o resultado no console
console.log(status_loja);

// O if ternário permite ao desenvolvedor criar uma estrutura condicional de if
// e else de forma simplificada










